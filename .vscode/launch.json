{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "request": "launch",
      "name": "Launch auth0-assistant",
      "command": "npm run dev",
      "cwd": "${workspaceFolder}/auth0-assistant0"
    }
  ]
}
